/**
 * @deprecated Use tenant.service — kept so older imports keep compiling during the API rewrite.
 */
export { default } from "./tenant.service";
export type { Tenant as Company } from "../types/models";
